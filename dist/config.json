{
	"webServer" : {
		"ipAddress" : "192.168.137.131",
		"port" : 8000
	},
	"dbConfig" : {
		"host" : "127.0.0.1",             // localhost, name.domain.com, 127.0.0.1
		"port" : 5632,
		"user" : "postgres",              // don't use default user and password, this is not secure
		"password" : "postgres",          // don't use default user and password, this is not secure
		"database" : "postgres",          // don't use default database, this is not secure
		"connectionReservation" : {
			"min" : 4,
			"max" : 8
		}
	},
	"logsConfig" : {
		"logsPath" : "logs",                     // need set relative path, absolute path not supported
		"errorFileName" : "errors.log",          // file name only, without path
		"warningFileName" : "warnings.log",      // file name only, without path
		"succesFileName" : "succes.log"          // file name only, without path
	}
}
